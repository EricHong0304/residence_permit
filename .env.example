# 示例环境变量（STAGING 说明）
#
# 在本项目中，staging 环境的配置文件位于：
#   - server/.env.staging（后端）
#   - app/.env.staging（前台）
#   - admin/.env.staging（管理端）
#
# 后端（server/.env.staging）：
#   PORT=3000                       # 后端端口
#   DB_PATH=./data/app.db           # SQLite 数据库文件相对路径（相对 server 目录）
#   CORS_ORIGINS=http://localhost:8080,http://localhost:8081  # 允许的 CORS 源
#
# 前端（app/.env.staging & admin/.env.staging）：
#   VITE_API_BASE_URL=http://localhost:3000  # 后端 API 基址，会在构建时注入
#
# 注意：
# - 运行 scripts/setup_verify_env.bat 将自动使用以上 staging 配置启动验证环境。
# - 如需变更端口或路径，请同步更新相关 .env.staging 与脚本。
